# Git Secrets Patterns
# Add these patterns to prevent committing secrets

# Database URLs
postgresql://[^:]+:[^@]+@
postgres://[^:]+:[^@]+@

# Supabase Project References
rmbcplfvucvukiekvtxb
\.supabase\.com

# API Keys
sk-[A-Za-z0-9]{32,}
OPENAI_API_KEY=[^\s]+

# Generic Secrets
password["\s]*[:=]["\s]*[^\s]+
secret["\s]*[:=]["\s]*[^\s]+
token["\s]*[:=]["\s]*[^\s]+

# Supabase Keys
eyJhbGciOi[A-Za-z0-9_-]+
